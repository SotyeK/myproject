<form method="post" action="{{ path('app_collectors_delete', {'id': collector.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collector.id) }}">
    <button class="btn">Delete</button>
</form>
